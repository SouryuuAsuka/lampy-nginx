
version: "3"
services:
  nginx:
    build: ../lampy-nginx/
    container_name: global-nginx

    ports:
      - 80:80
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"

networks:
  main-net:
      ipam:
        config:
        - subnet: 172.17.0.1/16